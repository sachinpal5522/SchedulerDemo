import styled from "styled-components";

export default styled.section`
  padding: 1rem;
  border-bottom: 1px solid var(--extra-light-font-color);
  color: var(--primary-color);
  display: flex;
  justify-content: space-between;
  align-items: center;
  .title {
    display: flex;
    h1 {
      margin-left: 0.5rem;
      color: var(--primary-font-color);
    }
  }
`;
